package com.class20;

public class Task1 {

	/*public static void main(String[] args) {
		//method with one parameters as a String
		//return----a reverse String
		//can be visible within the same package
		Task obj=new Task();
		String reversed=obj.reverseString("Hello");
		System.out.println(reversed);
		
     
	}
   protected String reverseString(String str) {
	   String reversed="";
	   for(int i=str.length()-1;i>0;i--) {
		   reversed=reversed+str.charAt(i);
	   }
	return reversed;
//--------------------------------------------------------------------	
	
}
   //public boolean isPalindrome(String str) {
	   String reverse="";
	   boolean flag=false;
	   for(int i=str.length()-1;i>0;i--) {
		reverse=reverse+str.charAt(i);
	}
	//if (reverse.equalsIgnore(str)) {
	//	flag true;
	}
	//---------------------------------------------------------------
   
   private String[] getWordsFromString(String str){
	   String[] words=str.split("");
	   return words;*/
	   
   }

